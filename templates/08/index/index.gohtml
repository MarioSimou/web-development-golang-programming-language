<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Hello world</title>
</head>
<body> 
    {{.}}
    <p>{{ index . 1}}</p>
    <p>{{ index . 2}}</p>
    <p>{{ index . 0}}</p>
    <p>{{ len . }}</p>
    <p>{{ index . 1 | printf "%T" }}</p>
</body>
</html>